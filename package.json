{
  "name": "homer-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Homer (working name): white-label, AI-native home platform. Monorepo root.",
  "workspaces": [
    "app",
    "portal"
  ],
  "scripts": {
    "dev:app": "npm run dev --workspace app",
    "dev:portal": "npm run dev --workspace portal",
    "build": "npm run build --workspace app && npm run build --workspace portal",
    "build:app": "npm run build --workspace app",
    "build:portal": "npm run build --workspace portal",
    "typecheck": "npm run typecheck --workspace app && npm run typecheck --workspace portal",
    "ci": "npm run typecheck && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
